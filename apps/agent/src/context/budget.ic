const budgetApi = fn() {
  fn create(config) {
    return {
      maxFiles: config.maxFiles,
      maxTotalBytes: config.maxTotalBytes,
      maxDepth: 3
    }
  }

  return {
    create: create
  }
}()

export {
  create: budgetApi.create,
}
